WATER BUDDY - PROGRESS LOG
============================
(See plan.txt for the full step list this log follows)

[2026-07-14] Plan written to plan.txt. Starting execution.

[2026-07-14] Step 1 DONE - Created requirements.txt (customtkinter>=5.2.0, Pillow>=10.0.0).

[2026-07-14] Step 2 DONE - Created assets/ folder + assets/README.txt listing the
             exact 9 filenames animations.py expects (idle.png, happy.png,
             angry.png, walk_in1-4.png, walk_out1-4.png).

[2026-07-14] Step 3 DONE - Fixed reminder.py timer bug:
             REMINDER_INTERVAL_MS: 5000 -> 45*60*1000 (45 min, matches docstring)
             SNOOZE_INTERVAL_MS:  10000 -> 5*60*1000  (5 min, matches docstring)

[2026-07-14] Step 3b DONE - Removed stray `print("ReminderManager exists...")`
             debug line that ran at import time in reminder.py.

[2026-07-14] Step 4 DONE - Added DesktopPetUI.USER_NAME = "Arwa" constant in
             ui.py and switched the greeting message to use it via f-string,
             so the name is now a one-line edit instead of buried in text.

[2026-07-14] Step 5 DONE - py_compile passed on all 4 files (no syntax errors).
             Installed dependencies from requirements.txt (customtkinter 6.0.0,
             darkdetect 0.8.0 pulled in as a sub-dependency; Pillow 12.2.0 was
             already present). Verified AnimationManager wiring is correct by
             instantiating it directly - it fails with exactly one expected
             error: "Missing asset: assets\idle.png". This confirms every other
             part of the app (imports, window setup, timer logic, button
             wiring) is in working order.

[2026-07-14] User uploaded a labeled sprite reference sheet (cat character,
             background already removed): Gemini_Generated_Image_jpq941jpq941jpq9-removebg-preview.png
             at project root. It contained all 11 needed poses in one image
             (idle/happy/angry/4x walk_in/4x walk_out), each captioned.

[2026-07-14] Step 6 DONE - Wrote a one-off cropper (numpy + PIL) that finds
             each sprite via alpha-channel row/column projection (the two
             densest horizontal bands are the sprite rows; column gaps split
             each row into individual sprites), tight-crops each to content,
             pads to a square, and saves as assets/idle.png, happy.png,
             angry.png, walk_in1-4.png, walk_out1-4.png. Verified visually via
             a contact sheet - all 11 crops are clean.

[2026-07-14] Verified AnimationManager loads all 11 real assets without
             error, then ran the actual app end-to-end (scripted, skipping
             the 45-min wait) and screenshotted each state. Found and fixed
             two real rendering bugs surfaced only by using real assets:

             BUG 1 - magenta fringe around the pet sprite. Root cause:
             animations.py resized frames to 220x220 with PIL's default
             bicubic filter, which blends the alpha channel at sprite edges;
             any partial-alpha edge pixel is not exactly the window's
             -transparentcolor key (#ff00ff) so Windows' colorkey
             transparency can't strip it, leaving a visible rim. Fix:
             animations.py now resizes with Image.NEAREST (no alpha
             blending) - appropriate anyway for pixel-art sprites.

             BUG 2 - Yes / "Remind Me In 5 Min" button text invisible
             (buttons rendered as flat color bars, no label). Root cause:
             NOT a customtkinter bug (reproduced identically with a plain
             tk.Button, and with customtkinter 6.0.0 and 5.2.2 both) - it's
             a Tk geometry-manager quirk. ui.py's hide_bubble()/show_bubble()
             used bubble_frame.pack_forget()/pack() to toggle the speech
             bubble (which contains the buttons). Whenever that pack_forget/
             pack cycle happened AND a sibling widget in the same pack-
             managed container (image_label) had its image changed via
             .configure(image=...), the re-shown frame's button text canvas
             came back blank. Confirmed via ~15 isolated repro scripts
             (see scratchpad, not kept in this repo) narrowing it down to
             exactly this combination. Fix: ui.py now positions bubble_frame
             and image_label with place()/place_forget() instead of pack()/
             pack_forget() at the container level, which avoids the sibling
             relayout cascade entirely. button_frame's own pack_forget()/
             pack() (show/hide the Yes/Snooze buttons within the bubble) was
             left as-is since it has no image-holding sibling and tested
             fine.

[2026-07-14] Re-ran the full end-to-end test after both fixes: verified all
             three bubble states render correctly with clean text and no
             fringe -- "Hey Arwa! Did you drink water?" with legible Yes /
             Remind Me In 5 Min buttons, "Good job!" (happy sprite, no
             buttons), and "Fine... I'll come back." (angry sprite, no
             buttons). Also swapped customtkinter 6.0.0 -> 5.2.2 (pinned in
             requirements.txt) since 6.0.0 is very new; 5.2.2 is the better-
             established release and the bug reproduced identically on both,
             so there was no reason to stay on the newer one.

STATUS: All 8 plan.txt steps are done. The app is fully working end-to-end
        with the user's real artwork. Remaining optional item is plan.txt
        step 8 (auto-start on Windows login), not started unless requested.

[2026-07-14] User renamed - USER_NAME in ui.py changed from "Arwa" to
             "Gaurav". Also changed reminder cadence: REMINDER_INTERVAL_MS
             in reminder.py changed from 45 min to 10 min (SNOOZE_INTERVAL_MS
             stays 5 min, matching the "Remind Me In 5 Min" button text).

[2026-07-14] Packaged as a standalone Windows app per user request ("want it
             like an app for my pc"):
             - Installed PyInstaller.
             - Generated assets/icon.ico (multi-res) from happy.png.
             - Built dist/Water_Buddy.exe via:
               python -m PyInstaller --onefile --windowed --icon="assets/icon.ico"
                 --add-data "assets;assets" --name "Water_Buddy" main.py
             - Verified the exe launches standalone (no Python needed) and
               successfully loads all 11 bundled assets without crashing -
               confirmed via EnumWindows (a "Water Buddy" window exists,
               IsWindowVisible=False, matching expected HIDDEN startup state)
               after the process stayed alive past the point where a missing-
               asset FileNotFoundError would have killed it immediately.
             - Created a "Water Buddy.lnk" shortcut on the Desktop
               (C:\Users\KIIT0001\Desktop\Water Buddy.lnk) pointing at the exe.
             - NOTE: PyInstaller build/dist folders and *.spec file were
               added to the project root by this step - not yet gitignored
               since this project has no git repo.

[2026-07-14] User confirmed they also want auto-start on login. Created a
             second shortcut, same target, in the Windows Startup folder:
             C:\Users\KIIT0001\AppData\Roaming\Microsoft\Windows\Start Menu\
             Programs\Startup\Water Buddy.lnk
             Water_Buddy.exe now launches automatically (silently, no
             console) every time this Windows user logs in.

STATUS: App works as a real double-click desktop app AND auto-starts on
        login. Both Desktop and Startup shortcuts point at
        dist\Water_Buddy.exe.

[2026-07-14] Published to GitHub per user request ("share as downloadable
             release"):
             - Installed GitHub CLI (winget) - user completed the browser
               device-code login themselves (account: Gauravpy-ctrl).
             - Added .gitignore (excludes __pycache__/, build/, dist/,
               .claude/, and the two original uploaded reference PNGs -
               those are superseded by the cropped assets/*.png).
             - git init + initial commit (22 files: source, assets,
               requirements.txt, Water_Buddy.spec, plan.txt, progress.txt).
             - Created public repo: https://github.com/Gauravpy-ctrl/water-buddy
             - Created Release v1.0.0 with dist/Water_Buddy.exe attached:
               https://github.com/Gauravpy-ctrl/water-buddy/releases/tag/v1.0.0

STATUS: Published. Repo + downloadable release are both live. No README.md
        was added (project convention: don't create doc files unless asked)
        - offered to the user as a follow-up, not done yet.

[2026-07-14] User asked for the reminder timer to be customizable through
             the app itself (not just source edits). Since the pet window
             is hidden most of the time, added a persistent entry point:

             - settings.py: Settings class, loads/saves
               reminder_minutes/snooze_minutes to
               %APPDATA%\WaterBuddy\config.json (survives restarts).
             - reminder.py: refactored ReminderManager off the old fixed
               *_INTERVAL_MS class constants -> instance-level
               reminder_minutes/snooze_minutes set via constructor, plus
               apply_new_intervals() which restarts whichever timer
               (reminder or snooze) is currently active so a change takes
               effect immediately, not just next cycle. Tracks
               _active_kind internally to know which timer is live.
             - settings_ui.py: SettingsDialog - small centered Toplevel,
               two Entry fields (reminder/snooze minutes), Save/Cancel,
               validates whole numbers >= 1.
             - tray.py: TrayIcon wraps pystray.Icon, right-click menu with
               "Set Reminder Timer..." and "Quit". Runs pystray's message
               loop on a daemon thread (Tkinter must stay single-threaded,
               so menu callbacks marshal back via root.after(0, ...)).
             - main.py: AppController now owns a Settings instance, passes
               its values into ReminderManager's constructor, starts the
               TrayIcon in run(), and exposes open_settings()/quit_app().
             - requirements.txt: added pystray>=0.19.0.

             Verified end-to-end with a scripted test (test_settings.py in
             scratchpad, not part of the repo): opened the dialog
             programmatically, changed values to 20/3, saved, confirmed
             ReminderManager picked up the new values live AND
             config.json was written correctly; then confirmed a fresh
             Settings() instance reloads 20/3 from disk. Reset the real
             config.json back to 10/5 afterward since the test wrote to
             the actual shared %APPDATA% path used by the installed exe.

[2026-07-14] Rebuilt and republished per user confirmation ("yes"):
             - No Water_Buddy.exe was running, so no need to kill anything
               before overwriting dist/Water_Buddy.exe.
             - Rebuilt with the same PyInstaller command as v1.0.0 (same
               --onefile --windowed --icon --add-data "assets;assets").
             - Verified the new exe launches without crashing and creates
               the expected hidden "Water Buddy" window (same EnumWindows
               check used for the original build).
             - Committed + pushed source (main.py, reminder.py,
               requirements.txt, settings.py, settings_ui.py, tray.py).
             - Created Release v1.1.0 with the rebuilt exe attached:
               https://github.com/Gauravpy-ctrl/water-buddy/releases/tag/v1.1.0
             - Did NOT need to touch the Desktop/Startup .lnk shortcuts -
               both point at dist\Water_Buddy.exe by path, which was
               overwritten in place, so they pick up the new build
               automatically next launch.

STATUS: Customizable timer feature is live in both the source repo and the
        installed exe (Desktop + Startup shortcuts). v1.1.0 is the current
        published release.

[2026-07-14] User asked for a custom-message feature ("custom alert their
             own msg"). Clarified scope with user: one custom message
             (not a rotating list) that replaces the default greeting.

             - settings.py: added custom_message field (default ""),
               load/save extended to persist it in the same
               %APPDATA%\WaterBuddy\config.json.
             - settings_ui.py: SettingsDialog gained a third field
               ("Custom message", blank = use default), on_save signature
               now (reminder_minutes, snooze_minutes, custom_message).
             - ui.py: show_question() takes an optional custom_message arg;
               falls back to the "Hey {USER_NAME}! Did you drink water?"
               default when blank.
             - main.py: enter_asking() passes self.settings.custom_message
               through; _apply_settings() updated for the new 3-arg save.

             Verified via scripted test (test_custom_msg.py in scratchpad):
             set a custom message directly through Settings, triggered the
             reminder, confirmed the bubble showed the custom text with
             correct wrapping and working buttons. Reset config back to
             defaults (10/5/"") afterward.

[2026-07-14] Rebuilt and republished per user confirmation ("yess"):
             - Found a live Water_Buddy.exe running (auto-started from the
               Startup shortcut) - killed it first since PyInstaller can't
               overwrite a locked exe.
             - Rebuilt with the same PyInstaller command as before.
             - Verified the new exe launches and creates the expected
               hidden "Water Buddy" window (EnumWindows check).
             - Committed + pushed source (main.py, settings.py,
               settings_ui.py, ui.py).
             - Created Release v1.2.0 with the rebuilt exe attached:
               https://github.com/Gauravpy-ctrl/water-buddy/releases/tag/v1.2.0
             - Relaunched Water_Buddy.exe afterward since an instance had
               been running (via Startup) before the rebuild forced it
               closed - restored to the state the user had it in.

STATUS: Custom message + customizable timer are both live in source and the
        installed exe. v1.2.0 is the current published release.

[2026-07-17] User asked for another upgrade. Discussed a "custom sticker"
             idea first (exploratory) - recommended letting users replace
             the static poses only, keeping default walk animation, as the
             simple option vs. full custom animation frames. User pushed
             for the fuller version: also wants to supply their own
             walk-in/walk-out animation PNGs. Asked how frames should be
             provided (individual file pickers vs. one labeled reference
             sheet like the original cat art vs. pointing at a folder) -
             user chose individual file pickers as most reliable.

             Implementation:
             - image_customizer.py (new): prepare_custom_image() fits any
               arbitrary user image (any size/aspect/format) onto a
               transparent 220x220 canvas via thumbnail+center-paste, then
               binarizes alpha to hard 0/255 - reusing the exact fringe fix
               from animations.py (soft edges + the window's colorkey
               transparency = visible rim otherwise).
             - settings.py: added custom_assets_dir property
               (%APPDATA%\WaterBuddy\custom_assets), auto-created.
             - animations.py: AnimationManager now takes an optional
               custom_assets_dir; _resolve_path() checks it before falling
               back to the bundled assets_dir, so each of the 9 files
               (idle/happy/angry/4x walk_in/4x walk_out) can be
               independently overridden or left default. Added reload() so
               changing images takes effect without restarting the app
               (init now just calls reload()).
             - image_settings_ui.py (new): ImagePickerDialog - one row per
               pose (Idle/Happy/Angry/Walk-in 4 frames/Walk-out 4 frames)
               showing Default/Custom status + a "Choose..." button using
               filedialog.askopenfilenames (multi-select for the two
               4-frame poses, relies on Windows preserving ctrl+click
               selection order - noted in the dialog's hint text). Also has
               "Reset All to Default" (deletes everything under
               custom_assets/).
             - tray.py: added "Customize Pet Images..." menu item between
               the timer settings and Quit.
             - main.py: wires settings.custom_assets_dir into
               AnimationManager at construction, opens ImagePickerDialog
               from the new tray callback, on_change=animations.reload.

             Verified: prepare_custom_image() tested on the large original
             (non-transparent, non-square) Gemini reference PNG - output
             was a clean 220x220 RGBA with only {0, 255} alpha values, no
             partial-transparency edges. ImagePickerDialog's internal logic
             tested by bypassing the real OS file dialog (can't be driven
             headlessly) and calling prepare_custom_image() directly for a
             4-frame walk_in override using the bundled walk_out sprites as
             stand-in source images - confirmed files landed correctly
             named in custom_assets/, status flipped to "Custom", and
             AnimationManager.reload() picked them up
             (controller.animations.idle became a fresh PhotoImage with no
             errors). A full visual walk-in screenshot during this test
             came back solid black (likely a transient capture issue, not
             re-investigated since the underlying override mechanism was
             already independently confirmed via the file-placement and
             reload checks above) - not treated as a blocking failure.

[2026-07-17] Rebuilt and republished per user confirmation ("yes"):
             - Found a live Water_Buddy.exe (Startup auto-launch) - killed
               it before rebuilding, same as the v1.2.0 cycle.
             - Rebuilt with the same PyInstaller command as before.
             - Verified the new exe launches, creates the expected hidden
               "Water Buddy" window, AND confirmed the user's real
               config.json (20 min reminder, custom message "dont forget to
               breathe") was untouched by any of the testing above - the
               dev-time tests only ever wrote to custom_assets/, never to
               config.json, so no reset-to-defaults was needed this round.
             - Committed + pushed source (animations.py, main.py,
               settings.py, tray.py, image_customizer.py,
               image_settings_ui.py).
             - Created Release v1.3.0 with the rebuilt exe attached:
               https://github.com/Gauravpy-ctrl/water-buddy/releases/tag/v1.3.0

STATUS: Custom pet images, custom message, and customizable timer are all
        live in source and the installed exe. v1.3.0 is the current
        published release.
